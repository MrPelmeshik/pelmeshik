import {useState} from "react";
import {useApi} from "./useApi";
import {RequestTypeEnum} from "../types/RequestTypeEnum";
import {CatalogTypeEnum} from "../components/_catalogs/CatalogTypeEnum";

export const useChangeTableData = <T,>(catalogType: CatalogTypeEnum) => {
    /*const [changedRows, setChangedRows] = useState<T[]>([]);
    const [deletedRows, setDeletedRows] = useState<T[]>([]);
    const [addedRows, setAddedRows] = useState<T[]>([]);
    const saveChanges = () => {
        return
    }

    return handleTableDataChange;*/
};
